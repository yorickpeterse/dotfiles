# These settings can be applied using the following command:
# $ git config --global core.excludesfile ~/.gitignore_global

# Generic files that don't have to be tracked by Git.
*.log

# Vim specific files.
.vimrc
*.swo
*.swp
tags

# OS specific files.
.DS_Store
Thumbs.db
Icon?

# Ruby specific files.
.yardoc
*.gem
