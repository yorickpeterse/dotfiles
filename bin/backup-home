#!/usr/bin/env bash
source /home/yorickpeterse/.bashrc

notify-send "Duply Backup" \
    "Duply will now create a backup of your home directory"

duply home backup now 2>&1 > /dev/null

notify-send "Duply Backup" "Finished backing up your home directory"
