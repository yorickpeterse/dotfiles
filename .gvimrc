" ============================================================================
" GVIM CONFIGURATION OPTIONS
"
" Configuration options specific to GUI versions of Vim (Gvim, Macvim, etc).
"

" Hide the toolbar/menu bars. I never use these and they occupy quite a bit of
" space.
set guioptions-=T
set guioptions-=m
